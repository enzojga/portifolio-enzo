export type education = {
    name: string,
    experience: string,
    tecnologies: any[],
    image: string
}

export type project = {
    name: string,
    description: string,
    icons: string[],
    images: string[],
    backgroundColor: string,
    fontColor: string,
    repositorie: string
}